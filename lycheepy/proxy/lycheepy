http {
    upstream repository {
        server repository:80 fail_timeout=10s max_fails=10;
    }

    server {
        listen 80;

        #location /configuration/ {
        #    proxy_pass http://configuration/;
        #}

        #location /wps/ {
        #    proxy_pass http://wps/;
        #}

        location /repository/ {
            proxy_pass http://repository;
        }
    }
}
